import React, { useEffect, useState } from 'react'

function Test() {
  return (
    <div>
      <form onSubmit={handleSubmit}>
        <button>Click Me</button>
      </form>
    </div>
  )
}

export default Test